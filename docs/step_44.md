# Step 44 — /topic Command

1. Problem being solved in this step: Users want a fast command to start a new topic without navigating menus.
2. Proposed technical solution: Add a `/topic` command that opens the topic entry flow.
3. Implementation plan: Add a command handler in the study router; update help and unknown-command hints; register the command.
4. Example code or pseudocode (if relevant): `/topic -> show_topic_entry()`.
5. Risks and edge cases: None; same as “Start learning.”
6. Suggested next improvements: Add `/topic` to quick reply menus.
